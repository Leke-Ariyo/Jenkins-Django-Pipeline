pipeline {
    agent any
    environment {
        registry = "vahiwe/capstone"
        registryCredential = 'dockerhub'
        dockerImage = ''
    }
    stages {
         stage('Lint files') {
              steps {
                  sh 'ls'
              }
         }
    }
}